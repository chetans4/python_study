install dJango

pip install django=1.11 # the 1.11 version supports with python 2.7.

>django-admin startproject pyshop .

After commenting db config from settings.py because we don't need db at this stage run below to start dev server
> python manage.py runserver